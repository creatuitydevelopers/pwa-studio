query storeLocator($id: Int) {
        storeLocator(id: $id){
                storelocator_id,
                address,
                baseimage,
                latitude,
                longitude,
                tags
        }
}

